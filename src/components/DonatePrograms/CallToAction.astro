---
import Headline from "../shared/Headline.astro";
import Wrapper from "../shared/Wrapper.astro";
import Button from "../shared/Button.astro";
import "./callToAction.scss";
import { Image } from "astro:assets";
---

<div class="donatepro-cta">
  <Wrapper>
    <div class="donatepro-cta-content">
      <div class="donatepro-cta-content-title">
        <Headline ele="h2" hlstyle="one">{Astro.props.data.title}</Headline>
      </div>
      <div
        class="donatepro-cta-content-para"
        set:html={Astro.props.data.content}
      />
      <div class="donatepro-cta-content-link">
        <Button btnstyle="two" href={Astro.props.data.buttonUrl}>
          {Astro.props.data.buttonText}
        </Button>
      </div>
    </div>
  </Wrapper>
  <div class="donatepro-cta-bg">
    <Image
      src={Astro.props.data.backgroundImage.sourceUrl}
      alt={Astro.props.data.backgroundImage.altText}
      inferSize={true}
    />
  </div>

  <div class="donatepro-cta-overlay"></div>
</div>
